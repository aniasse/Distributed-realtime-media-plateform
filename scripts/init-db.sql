-- DRMP Database Initial Schema
CREATE TABLE IF NOT EXISTS rooms (
    id UUID PRIMARY KEY,
    tenant_id UUID NOT NULL,
    max_participants INTEGER NOT NULL,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE IF NOT EXISTS peers (
    id UUID PRIMARY KEY,
    room_id UUID REFERENCES rooms(id) ON DELETE CASCADE,
    connected_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP
);

-- Insérer une salle de test pour notre premier essai
INSERT INTO rooms (id, tenant_id, max_participants) 
VALUES ('00000000-0000-0000-0000-000000000000', '00000000-0000-0000-0000-000000000000', 10)
ON CONFLICT DO NOTHING;
