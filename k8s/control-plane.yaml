apiVersion: apps/v1
kind: Deployment
metadata:
  name: control-plane
  namespace: drmp
  labels:
    app: control-plane
    tier: management
spec:
  replicas: 2
  selector:
    matchLabels:
      app: control-plane
  template:
    metadata:
      labels:
        app: control-plane
        tier: management
    spec:
      containers:
      - name: control-plane
        image: drmp/control-plane:latest
        ports:
        - containerPort: 8080
          name: api
        env:
        - name: RUST_LOG
          value: "info"
        - name: DATABASE_URL
          value: "postgresql://postgres:password@postgres:5432/drmp"
        resources:
          requests:
            cpu: "250m"
            memory: "256Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5

---
apiVersion: v1
kind: Service
metadata:
  name: control-plane
  namespace: drmp
  labels:
    app: control-plane
    tier: management
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    name: api
  selector:
    app: control-plane